<li class='feed_item'>
    <div class='feed_abstract_img'><img src='<%=notification.about.shop.user_proxy.picture%>'/></div>
    <div class='feed_abstract_dismiss symbol' data-db-notif_id='<%=notification.id%>'>'</div>
    <a href='<%=notification.about.shop.fullpermalink(:locale)%>/review'>
      <div class='feed_abstract_title'><strong><%=notification.about.shop.user_proxy.nickname%></strong> has replied to your review.</div>
    </a>
    <div class='feed_abstract_when'>
      <%if !notification.when['days'].nil? then%><%=notification.when['days']%> days ago
      <%elsif !notification.when['hours'].nil? then%><%=notification.when['hours']%> hours ago
      <%elsif !notification.when['minutes'].nil? then%><%=notification.when['minutes']%> minutes ago
      <%elsif !notification.when['seconds'].nil? then%><%=notification.when['seconds']%> seconds ago
      <%end%>
    </div>
  </a>
</li>
