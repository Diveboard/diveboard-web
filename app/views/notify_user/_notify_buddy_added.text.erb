<%= it("Hi %{name} !", scope: ['notify_user', 'notify_buddy_added', 'text']) %>

<%= it("Your buddy %{buddy} wanted to let you know that he just logged the dive you did together in %{spot} on %{date}.", scope: ['notify_user', 'notify_buddy_added', 'text'], buddy: @b_owner.nickname, spot: @b_dive.spot.name, date: @b_dive.date) %>

<%= it("You should check it out here : %{txt_url}", scope: ['notify_user', 'notify_buddy_added', 'text'], txt_url: @b_dive.fullpermalink(:locale)) %>

<%= it("Happy Diving !", scope: ['notify_user', 'notify_buddy_added', 'text']) %>
--
<%= it("The Diveboard team", scope: ['notify_user', 'notify_buddy_added', 'text']) %>
http://www.diveboard.com
